// Generated/append-only spatial anchors promoted from runtime geocoding.
// Existing lookupKey entries must never be rewritten automatically.
// The daily exporter appends only previously unseen keys.
export const LEARNED_ADDRESS_ENTITIES = Object.freeze([]);
